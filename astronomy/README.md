# astronomy

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build astronomy` to build the library.

## Running unit tests

Run `nx test astronomy` to execute the unit tests via [Jest](https://jestjs.io).
